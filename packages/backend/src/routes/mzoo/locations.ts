/**
 * Location-related routes for MZOO services
 */

import { Router } from 'express';
import { HTTP_STATUS } from '../../config';

const router = Router();

// No active routes - reserved for future location-related endpoints

export default router;
