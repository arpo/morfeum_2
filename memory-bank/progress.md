# Progress

## 2026-01-12 - V2 Code Cleanup & Modularization ✅

Major refactoring to split large files and remove dead code.

**Changes:**

1. **promptBuilder.ts** - Removed dead code
   - ~265 → ~70 lines
   - Removed unused: `PromptLayers`, `buildHostImagePrompt()`, `buildRegionImagePrompt()`, `buildLocationImagePrompt()`, etc.
   - Kept: `cascadeDNA()`, `CascadedDNAChain` (actually used)

2. **v2Commands.ts (Frontend)** - Split into modules
   - 431 lines → 8 files (~50-85 lines each)
   - New structure: `commands/{index,types,handlers/*,utils/*}`

3. **routes.ts (Backend)** - Split into modules
   - 471 lines → 7 files (~35-130 lines each)
   - New structure: `handlers/*`, `utils/routeUtils.ts`

---

## 2026-01-12 - V2 DISPLAY Image Auto-Load Fix ✅

Fixed bug where images generated by `/DISPLAY` weren't auto-loading.

**Issue:** Generated images only appeared after page reload
**Fix:** Added `imageGenerated` event dispatch to trigger WorldView refresh

---

## World V2 System

### Phase 1-4: ✅ COMPLETE

| Command | Description | Status |
|---------|-------------|--------|
| `/NEW_HOST` | Create world with DNA | ✅ |
| `/NEW_REGION2` | Create region (delta DNA) | ✅ |
| `/NEW_LOCATION2` | Create location (delta DNA) | ✅ |
| `/DISPLAY` | Generate image via LLM layers | ✅ |

### Phase 5-6: 🚧 PENDING

- [ ] Navigation for V2 nodes (GO_INSIDE, GOTO)
- [ ] Remove old system

---

## V2 Files Structure

**Backend:**
```
packages/backend/src/worldV2/
├── routes.ts             # Router (~32 lines)
├── handlers/             # Route handlers
├── utils/routeUtils.ts   # Shared utilities
├── prompts/              # DNA generation prompts
└── display/              # /DISPLAY command
```

**Frontend:**
```
packages/frontend/src/worldV2/
├── commands/
│   ├── index.ts          # Main exports
│   ├── types.ts
│   ├── handlers/         # Command handlers
│   └── utils/            # Shared utilities
└── index.ts
```

---

## What Works ✅

- V2 World System (NEW_HOST, NEW_REGION2, NEW_LOCATION2, DISPLAY)
- Gemini caching (morfeum-world-creation, morfeum-navigation)
- Core features (commands, entities, world tree, navigation, image editing)

---

## What's Left 🚧

- [ ] V2 Navigation (GO_INSIDE, GOTO)
- [ ] Remove old system
- [ ] Character spawn caching test
- [ ] Bundle size optimization

---

## Known Issues 🐛

- Bundle size warning (865KB)
- FLUX 1 may ignore constraints
- DNA generation too dynamic for caching
